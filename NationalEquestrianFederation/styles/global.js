import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1
    },
    titleText: {
        fontSize: 18,
        fontWeight: 'bold',
        fontStyle: 'italic'
    },
    titleDate: {
        fontSize: 12,
        color: '#333',
        textAlign: 'right',
        marginTop: -3,
    },
})